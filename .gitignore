node_modules/
dist/
.rig/sessions/
.rig/cache/
.rig/provider.json
.rig/model.json
.rig/config.json
.DS_Store

# Local agent workspace (memory, skills) — never publish to the public repo
.workbuddy-ai/
